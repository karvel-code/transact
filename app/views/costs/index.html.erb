<h1 class="text-center">Grouped Cost </h1>

<% @all_groups.each do |group| %>
    <% total_cost = 0 %>
    <% group.costs.each do |cost| %>
        <% if cost.author_id == current_user.id %>
            <div class= "d-flex justify-content-center grouped-cost-section">
                <p class="mx-2"> Name: <%= cost.name %> </p>
                <p class="mx-2">Group: <%= cost.groups.to_a.first.name %></p>
                <p> Amount: <%= cost.amount %> </p>
                <% total_cost += cost.amount %>
            </div>
        <%end%>
    <%end %>
    <div class= "d-flex justify-content-center grouped-cost-section">
        <% unless total_cost == 0 %>
            <p> Total: <%= total_cost %> </p>
        <% end %>
    </div>
    
  <% end %>